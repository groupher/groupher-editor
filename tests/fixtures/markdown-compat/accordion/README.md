# Accordion compatibility fixtures

These fixtures define the source-independent `accordion_group` contract used by
the Markdown importer. Platform adapters may recognize different MDX or HTML
syntax, but every successful import must produce the same nested Plate nodes.
